# DA Review — Rich Issue Model Implementation Plan

**Date:** 2026-04-09
**Status:** All 35 findings incorporated into plan v2
**Reviewer:** Code Architect (feature-dev:code-architect)

## Summary

35 findings: 6 CRITICAL (execution blockers), 16 HIGH, 10 MEDIUM, 3 LOW. All incorporated into plan v2.

## Critical Blockers (resolved in v2)

1. NULL uniqueness in catalog indexes → partial unique indexes
2. FK constraints deferred on entity tables → immediate FK wiring
3. NOT NULL columns on existing data → Phase 0 nuke before schema push
4. userId NOT NULL on existing project → Phase 0 nuke
7. Circular imports between split schema files → single schema.ts
13. FOR UPDATE not in Drizzle ORM → raw SQL in transaction
23. createTRPCContext() outside request handler → direct DB import

## High Severity (resolved in v2)

5. Dependency table missing FKs → immediate FK wiring
6. Transition table NULL uniqueness → partial unique index
8. JSONB array default syntax → sql template literal
9. Backfill step missing → not needed (nuked DB)
10. Config scoping → projectId added to configEntry
11. Nuke deletion order → explicit FK-safe order
15. Initial state unspecified → lowest sortOrder, non-terminal
16. close/reopen semantics → override (bypass graph)
17. Bulk operations too vague → deferred
20. 5 routers in one task → nested in issue router (1 task, but structured)
21. transitions endpoint two-service call → method on issue service
22. Router namespace mismatch → nested sub-routers
25. Hardcoded redirect → dynamic DB query
26. Mutation queue unspecified → save-on-blur for v1
31. Auto-fix violations → stop and report

## Medium Severity (resolved in v2)

12. Seed idempotency → explicit conflict targets
19. Catalog list exposes inactive → override list()
27. Assignee source ambiguous → free text, SELECT DISTINCT
28. Test isolation → unique slugs per run
29. Missing catalog tests → added before issue tests
30. Pipeline stages in seed → retained, allowed per CLAUDE.md
32. Pipeline status defaults → known debt, not this plan
33. Comment migration → not needed (nuked DB)
34. gates/types.ts deletion → not needed (no issue state refs)
35. Kanban deferred decision → explicit "not in this plan"
