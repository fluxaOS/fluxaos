# Session Start Prompt — R5.5 Signal Handling & Skill Cleanup

Copy and paste this at the start of the next session.

---

## Prompt

We're continuing work on fluxaOS. Please read the full session handoff before doing anything else:

`docs/superpowers/handoffs/2026-04-14-r5.5-signal-handling-session.md`

Also read:
- `docs/issue-lifecycle.md` — states, statuses, transitions, skill signal protocol
- `CLAUDE.md` — project commands, architecture, key principles

Once you've read those, confirm you understand the 6 outstanding tasks and their order, then we'll begin.

**Do not start any implementation until I confirm we're aligned on the plan.**

---

## What to Confirm Before Starting

After reading the handoff, confirm:

1. **Task order** — Tasks 1→2→3→4→5→6 in sequence. Tasks 2 and 3 can be done in parallel (same logic, different files). Tasks 5 and 6 are independent of 1-4 and can be done after.

2. **`stateOverride()` is the right method** — located at `src/core/services/issue.ts:460`. Bypasses transition table. Requires current issue version (fetch fresh before calling).

3. **Seed must be re-run after any seed.ts change** — always `npx tsx src/core/db/nuke.ts && npm run db:seed` to pick up changes.

4. **No new DB transitions needed** — the `hold/already_complete` path uses `stateOverride()`, not the transition table. Do not add `research → complete` or similar transitions to the seed.

5. **Skills in DB are wrong** — the current `promptTemplate` values are full interactive SKILL.md content. Task 5 fixes this. Until then, stage runs will waste tokens and may behave unexpectedly.

6. **Gates tab is empty** — this is a known bug (Task 4). Don't investigate it as a new issue.
