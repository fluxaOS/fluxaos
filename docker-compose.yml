services:
  fluxaos:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://postgres:supersecure@host.docker.internal:5432/fluxaos
      REDIS_URL: redis://:${REDIS_PASSWORD}@host.docker.internal:6379
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      FLUXAOS_GIT_PROVIDER: ${FLUXAOS_GIT_PROVIDER:-github}
      FLUXAOS_AUTH_PROVIDER: ${FLUXAOS_AUTH_PROVIDER:-supabase}
      FLUXAOS_REALTIME_PROVIDER: ${FLUXAOS_REALTIME_PROVIDER:-supabase}
      FLUXAOS_QUEUE_PROVIDER: ${FLUXAOS_QUEUE_PROVIDER:-bullmq}
      FLUXAOS_STAGE_EXECUTOR: ${FLUXAOS_STAGE_EXECUTOR:-node-exec}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - homelab
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

networks:
  homelab:
    external: true
