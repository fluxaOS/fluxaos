---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - src/server/trpc.ts
  - src/server/root.ts
  - src/server/routers/health.ts
  - src/server/routers/organization.ts
  - src/server/routers/project.ts
  - src/server/routers/pipeline.ts
  - src/server/routers/issue.ts
  - src/server/routers/persona.ts
  - src/server/routers/skill.ts
  - src/server/routers/provider.ts
  - src/app/api/trpc/[trpc]/route.ts
  - src/lib/trpc/client.ts
  - src/lib/trpc/provider.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/issues/page.tsx
  - src/app/(dashboard)/pipelines/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/components/nav.tsx
  - .github/workflows/ci.yml
  - vitest.config.ts
  - src/__tests__/health.test.ts
autonomous: true
---

<objective>
## Goal
Wire up tRPC with empty domain routers, add a CI pipeline (lint + type-check + vitest), and create the basic app shell with navigation. This completes Phase 1 — the full skeleton is standing.

## Purpose
Phase 1 exit criteria require: tRPC health check returns OK, CI passes, and the app shell renders with navigation. This plan delivers all three, making the codebase ready for Phase 2 business logic.

## Output
- tRPC server with 8 domain routers (empty stubs) + health router (working)
- tRPC client wired into React via TanStack Query provider
- GitHub Actions CI workflow: lint, type-check, test
- App shell with sidebar nav and route group layout
- One passing test proving the setup works
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-foundation/01-03-SUMMARY.md
- Port interfaces and adapter registry established — tRPC routers will import types from `@/core/ports/`
- Schema tables map to tRPC router domains

## Source Files
@src/core/db/schema.ts (21 tables — informs router domains)
@src/core/ports/index.ts (port types for router input/output)
@src/config/registry.ts (adapter registry — routers will use this)
@src/app/layout.tsx (current root layout — will be modified)
@package.json (tRPC deps already installed: @trpc/server, @trpc/client, @trpc/react-query, @trpc/next)
@tsconfig.json (path aliases configured)
</context>

<acceptance_criteria>

## AC-1: tRPC health check returns OK
```gherkin
Given the Next.js dev server is running
When a client calls trpc.health.check.query()
Then it returns { status: "ok", timestamp: <ISO string> }
```

## AC-2: All domain routers registered on appRouter
```gherkin
Given the tRPC appRouter is defined
When inspecting its type signature
Then it includes routers for: health, organization, project, pipeline, issue, persona, skill, provider
And each router exports at least one placeholder procedure
```

## AC-3: CI pipeline passes
```gherkin
Given the GitHub Actions workflow exists at .github/workflows/ci.yml
When triggered on push or pull_request
Then it runs: biome check, tsc --noEmit, vitest run
And all three steps pass
```

## AC-4: App shell renders with navigation
```gherkin
Given the app is running at localhost:3000
When navigating to /
Then a sidebar/nav shows links to: Dashboard, Issues, Pipelines, Settings
And the page renders with the fluxaOS layout (dark theme, brand colors)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: tRPC server setup + domain routers</name>
  <files>
    src/server/trpc.ts,
    src/server/root.ts,
    src/server/routers/health.ts,
    src/server/routers/organization.ts,
    src/server/routers/project.ts,
    src/server/routers/pipeline.ts,
    src/server/routers/issue.ts,
    src/server/routers/persona.ts,
    src/server/routers/skill.ts,
    src/server/routers/provider.ts,
    src/app/api/trpc/[trpc]/route.ts,
    src/lib/trpc/client.ts,
    src/lib/trpc/provider.tsx,
    tsconfig.json
  </files>
  <action>
    1. Create `src/server/trpc.ts`:
       - Initialize tRPC with `initTRPC.create()` (superjson transformer)
       - Export `router`, `publicProcedure`, `createCallerFactory`
       - Add `@/server` path alias to tsconfig.json if not present

    2. Create `src/server/routers/health.ts`:
       - Single `check` query returning `{ status: "ok", timestamp: new Date().toISOString() }`

    3. Create 7 domain routers as empty stubs (organization, project, pipeline, issue, persona, skill, provider):
       - Each exports a router with one `list` query that returns an empty array
       - Input: none. Output: empty array. Placeholder for Phase 2+ business logic.

    4. Create `src/server/root.ts`:
       - Merge all routers into `appRouter`
       - Export `AppRouter` type

    5. Create `src/app/api/trpc/[trpc]/route.ts`:
       - Next.js App Router handler using `fetchRequestHandler`
       - Wire to `appRouter`

    6. Create `src/lib/trpc/client.ts`:
       - `createTRPCReact<AppRouter>()` export
       - httpBatchLink pointing to `/api/trpc`

    7. Create `src/lib/trpc/provider.tsx`:
       - "use client" TRPCProvider wrapping QueryClientProvider + trpc.Provider
       - Create QueryClient + trpc client inside component

    Avoid: importing anything from adapters/ in server/routers/ — routers are stubs for now.
    Avoid: superjson import issues — use dynamic import or conditional if needed.
  </action>
  <verify>
    - `tsc --noEmit` passes with zero errors
    - `curl localhost:3000/api/trpc/health.check` returns `{"result":{"data":{"status":"ok","timestamp":"..."}}}`
  </verify>
  <done>AC-1 satisfied: health check returns OK. AC-2 satisfied: all 8 routers on appRouter.</done>
</task>

<task type="auto">
  <name>Task 2: CI pipeline + first test</name>
  <files>
    .github/workflows/ci.yml,
    vitest.config.ts,
    src/__tests__/health.test.ts
  </files>
  <action>
    1. Create `.github/workflows/ci.yml`:
       - Trigger: push + pull_request on main
       - Node 22, pnpm or npm (match lockfile)
       - Steps: checkout, setup-node, install deps, `npx biome check .`, `tsc --noEmit`, `npx vitest run`
       - Single job is fine for alpha

    2. Verify `vitest.config.ts` exists (created in 01-01). If not, create it:
       - Configure with path aliases matching tsconfig.json
       - Environment: node (not jsdom — server tests)

    3. Create `src/__tests__/health.test.ts`:
       - Import `createCallerFactory` and `appRouter`
       - Create caller via `createCallerFactory(appRouter)({})`
       - Test: `health.check` returns object with `status: "ok"` and `timestamp` string
       - This validates the entire tRPC stack without HTTP

    Avoid: Adding Playwright E2E tests — those are Phase 7.
    Avoid: Using `npm ci` if repo uses another package manager — check lockfile.
  </action>
  <verify>
    - `npx vitest run` passes with 1 test
    - `.github/workflows/ci.yml` is valid YAML (check with `cat` + visual inspection)
  </verify>
  <done>AC-3 satisfied: CI workflow exists and all three checks pass locally.</done>
</task>

<task type="auto">
  <name>Task 3: App shell with navigation</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/(dashboard)/layout.tsx,
    src/app/(dashboard)/page.tsx,
    src/app/(dashboard)/issues/page.tsx,
    src/app/(dashboard)/pipelines/page.tsx,
    src/app/(dashboard)/settings/page.tsx,
    src/components/nav.tsx,
    src/app/globals.css
  </files>
  <action>
    1. Update `src/app/layout.tsx`:
       - Set metadata: title "fluxaOS", description matching core value
       - Wrap children with TRPCProvider from `@/lib/trpc/provider`
       - Apply dark theme base (dark background, light text via Tailwind)

    2. Create `src/components/nav.tsx`:
       - "use client" sidebar nav component
       - Links: Dashboard (/), Issues (/issues), Pipelines (/pipelines), Settings (/settings)
       - Use Tailwind for styling — dark sidebar, brand violet accent for active state
       - Simple text links, no icons needed for alpha

    3. Create `src/app/(dashboard)/layout.tsx`:
       - Route group layout with sidebar nav + main content area
       - Flexbox: nav on left (w-64), content fills remaining space

    4. Create route pages as minimal placeholders:
       - `(dashboard)/page.tsx` — "Dashboard" heading, brief description
       - `(dashboard)/issues/page.tsx` — "Issues" heading
       - `(dashboard)/pipelines/page.tsx` — "Pipelines" heading
       - `(dashboard)/settings/page.tsx` — "Settings" heading

    5. Update `src/app/page.tsx`:
       - Redirect to dashboard or render dashboard content directly

    Avoid: Complex UI components — these are placeholders for Phase 5.
    Avoid: Fetching real data — Phase 2 adds actual CRUD.
    Use brand colors from branding work: violet accents (#7C3AED / violet-600), dark backgrounds.
  </action>
  <verify>
    - `next build` succeeds
    - Navigate to localhost:3000 — sidebar visible with 4 nav links
    - Each nav link renders its placeholder page
  </verify>
  <done>AC-4 satisfied: app shell renders with navigation and dark theme.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/core/db/schema.ts` (schema locked — completed in 01-02)
- `src/core/ports/*` (port interfaces locked — completed in 01-03)
- `src/config/registry.ts` (adapter registry locked — completed in 01-03)
- `src/adapters/supabase/*` (Supabase adapter locked — completed in 01-03)
- `docker-compose.yml` (infrastructure config stable)

## SCOPE LIMITS
- tRPC routers are **empty stubs** — no business logic, no database queries
- No auth middleware on routes yet — Supabase auth middleware is a Phase 2 concern
- No real data fetching in UI — placeholder pages only
- No Playwright E2E tests — unit/integration tests only
- No additional npm dependencies beyond what's already in package.json

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `tsc --noEmit` — zero errors
- [ ] `npx biome check .` — passes (or only pre-existing warnings)
- [ ] `npx vitest run` — health test passes
- [ ] `curl localhost:3000/api/trpc/health.check` — returns OK response
- [ ] App renders at localhost:3000 with sidebar navigation
- [ ] All 4 nav links route to placeholder pages
- [ ] No vendor imports in `src/server/` (routers are pure stubs)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All 4 acceptance criteria pass
- Zero type errors, lint passes, test passes
- App shell visually functional with dark theme and navigation
- Phase 1 exit criteria achievable: app starts, health check works, DB tables exist
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation/01-04-SUMMARY.md`
Phase 1 complete — trigger phase transition after UNIFY.
</output>
